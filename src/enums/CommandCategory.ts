/**
 * This enum defines the categories a command can fall within
 */

enum CommandCategory {
	DEV = 'Development',
	MOD = 'Moderation',
	INFO = 'Bot information',
	// Will add more as more commands are written
}

export default CommandCategory;
